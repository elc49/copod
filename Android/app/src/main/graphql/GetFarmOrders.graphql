query GetFarmOrders($id: UUID!) {
    getFarmOrders(id: $id) {
        id
        currency
        volume
        toBePaid
        market {
            id
            name
            image
            unit
        }
        customer {
            id
            phone
        }
        status
        created_at
    }
}