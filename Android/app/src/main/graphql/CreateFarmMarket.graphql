mutation CreateFarmMarket($input: NewFarmMarketInput!) {
    createFarmMarket(input: $input) {
        id
        name
        image
        unit
        pricePerUnit
        volume
        farmId
    }
}