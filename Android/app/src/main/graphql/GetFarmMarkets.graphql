query GetFarmMarkets($input: GetFarmMarketsInput!) {
    getFarmMarkets(input: $input) {
        id
        name
        image
        farmId
        status
        type
        unit
        running_volume
        volume
        pricePerUnit
    }
}