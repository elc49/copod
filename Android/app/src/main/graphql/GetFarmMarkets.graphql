query GetFarmMarkets($id: UUID!) {
    getFarmMarkets(id: $id) {
        id
        name
        image
        farmId
        status
        unit
        running_volume
        volume
        pricePerUnit
    }
}