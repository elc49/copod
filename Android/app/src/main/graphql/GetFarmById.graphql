query GetFarmById($id: UUID!) {
    getFarmById(id: $id) {
        id
        name
        about
        rating
        reviewers
        completed_orders
        dateStarted
        thumbnail
        address_string
    }
}