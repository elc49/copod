query GetMarketDetails($id: UUID!) {
    getMarketDetails(id: $id) {
        id
        name
        details
        image
        unit
        volume
        running_volume
        type
        farmId
        farm {
            id
            name
        }
        pricePerUnit
    }
}