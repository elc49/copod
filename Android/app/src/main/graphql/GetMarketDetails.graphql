query GetMarketDetails($id: UUID!) {
    getMarketDetails(id: $id) {
        id
        name
        details
        image
        unit
        volume
        farmId
        farm {
            id
            name
        }
        pricePerUnit
    }
}