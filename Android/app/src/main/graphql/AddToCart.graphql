mutation AddToCart($input: AddToCartInput!) {
    addToCart(input: $input) {
        id
        farm_id
        market_id
        volume
        farm {
            id
            name
        }
        market {
            id
            image
            name
            unit
            pricePerUnit
        }
    }
}