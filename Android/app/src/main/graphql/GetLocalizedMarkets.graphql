query GetLocalizedMarkets($input: GetLocalizedMarketsInput!) {
    getLocalizedMarkets(input: $input) {
        id
        name
        image
        type
        canOrder
        status
        volume
        running_volume
        pricePerUnit
        farmId
        unit
    }
}