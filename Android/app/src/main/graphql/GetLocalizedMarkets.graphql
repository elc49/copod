query GetLocalizedMarkets($radius: GpsInput!) {
    getLocalizedMarkets(radius: $radius) {
        id
        name
        image
        canOrder
        status
        volume
        running_volume
        pricePerUnit
        farmId
        unit
    }
}