query GetLocalizedHarvestMarkets($radius: GpsInput!) {
    getLocalizedHarvestMarkets(radius: $radius) {
        id
        name
        image
        canOrder
        status
        volume
        running_volume
        pricePerUnit
        farmId
        unit
    }
}