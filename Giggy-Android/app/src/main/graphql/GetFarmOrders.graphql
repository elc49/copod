query GetFarmOrders($id: UUID!) {
    getFarmOrders(id: $id) {
        id
        currency
        volume
        toBePaid
        market {
            id
            name
            unit
        }
        customer {
            id
            phone
        }
        status
    }
}