query GetLocalizedPosters($radius: GpsInput!) {
    getLocalizedPosters(radius: $radius) {
        id
        text
        image
        userId
        tags
        farmAddress {
            addressString
        }
        user {
            avatar
            username
        }
        created_at
        updated_at
    }
}