mutation AddToCart($input: AddToCartInput!) {
    addToCart(input: $input) {
        id
        farm_id
        market_id
        volume
        market {
            image
            name
            pricePerUnit
        }
    }
}