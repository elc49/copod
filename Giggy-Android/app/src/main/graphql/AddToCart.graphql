mutation AddToCart($input: AddToCartInput!) {
    addToCart(input: $input) {
        id
        farm_id
        market_id
        volume
        farm {
            id
            name
        }
        market {
            image
            name
            pricePerUnit
        }
    }
}